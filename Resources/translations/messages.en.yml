example:
  header:
    title:
      collections: collections
      customBreadCrumb: custom bread crumb
      datepicker: datepickers
      hello: hello %name%
      labels: labels
      api: api

  menu:
    bootstrap: bootstrap
    collections: collections
    customBreadCrumb: custom bread crumb
    datepicker: datepickers
    examples: examples
    hello: hello
    labels: labels
    tutorials: tutorials
    statistics: statistics
    api: api

datepicker:
  birthday.example: birthday
  date.example: date
  datetime.example: datetime
  time.example: time

labels:
  label:
    example.with.help.block: with help block
    example.with.help.label: with help label
    example.with.tooltip: with tooltip
    example.with.tooltip.without.icon: with tooltip but no icon
    example.with.popover: with popover
    example.with.popover.and.options: with popover and options

collections:
  languages: languages
  languages.without.label: without a label

api:
  install: install the nelmioApiDocBundle
  composer: require the nelmioApiDocBundle using composer
  kernel: load the bundle in your AppKernel
  routing: add the routes to the bundle in your routing.yml file
  config: enable the configuration in your config.yml file
  use: create api documentation using the ApiDoc annotation

name:
  label: language
