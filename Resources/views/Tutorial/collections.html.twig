{% extends '::base.html.twig' %}

{% block header_title %}
    <h2>
        {{ 'example.header.title.collections'|trans|capitalize }}
    </h2>
{% endblock %}

{% block main %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
