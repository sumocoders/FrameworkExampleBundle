{% extends '::base.html.twig' %}

{% block header_title %}
  <h2>
    {{ 'example.header.title.sendMail'|trans|capitalize }}
  </h2>
{% endblock %}

{% block main %}
  {{ form_start(form) }}
  {{ form_rest(form) }}
  <div class="form-group">
    <div class="col-lg-12">
      <input type="submit" class="btn btn-primary" value="Ok">
    </div>
  </div>
  {{ form_end(form) }}
{% endblock %}
